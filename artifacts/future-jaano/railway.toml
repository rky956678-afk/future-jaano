[build]
builder = "nixpacks"
buildCommand = "npm install -g pnpm@9.15.4 && pnpm install --no-frozen-lockfile && pnpm --filter future-jaano build"

[deploy]
startCommand = "npx serve -s artifacts/future-jaano/dist -l $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3