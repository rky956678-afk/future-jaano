[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile && pnpm --filter future-jaano build"

[deploy]
startCommand = "npx serve -s artifacts/future-jaano/dist -l $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3