[variables]
NODE_VERSION = "22"

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@9.15.4 --activate",
  "pnpm install --no-frozen-lockfile"
]

[phases.build]
cmds = ["echo 'no build'"]

[start]
cmd = "node server.js"
