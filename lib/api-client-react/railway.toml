[build]
builder = "nixpacks"
buildCommand = "npm install -g pnpm@9.15.4 && pnpm install --no-frozen-lockfile"

[deploy]
startCommand = "node lib/api-client-react/server.mjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3